# MT5 fetcher configuration (Phase 0, standalone module).
# Copy to .env and fill in. Do not commit the real .env.
# Note: the existing .env.example in this directory is for the Docker/Yahoo
# stack (DB_HOST etc.) — this file is scoped only to mt5_data_fetcher.py.

MT5_PATH=C:\Program Files\Eightcap MetaTrader 5\terminal64.exe
MT5_LOGIN=
MT5_PASSWORD=
MT5_SERVER=
MT5_SYMBOL=XAUUSD
